import React from 'react';
import { Button, Grid } from '@mui/material';

type Props = {
  firstAdmin: boolean;
};

const Index = ({ firstAdmin }: Props) => {
  return <Grid container></Grid>;
};

export default Index;
